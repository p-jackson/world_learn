# Author the MVP spec

Type: task
Status: resolved
Blocked by: 04, 05, 06

## Question

Synthesise every resolved decision into the handoff-ready MVP spec document
(`.scratch/mvp-spec/spec.md`) — the destination of this map.

Pull together: the fixed premises (map Notes), the geometry pipeline (03), the
deck-derivation rules (05), the interaction/screen spec (04), the data model
(06), persistence (01), and FSRS scheduling (02). The result must let a build
session execute without further product decisions.

Not a decision ticket — it's the assembly. Resolve only once its blockers are.

## Answer

Spec authored at `.scratch/mvp-spec/spec.md`. Synthesises every resolved
decision — fixed premises (map Notes), deck derivation (05), geometry pipeline
(03), review loop/screens (04), data model + scheduling (06), persistence (01),
FSRS (02) — into 9 sections + a suggested build-phase order + out-of-scope.
Every product decision is settled; each choice carries its fallback. This is
the destination — the map is complete.
